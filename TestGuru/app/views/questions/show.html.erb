<h1>Show questions </h1>

<%= form_with model: [@question] do |form| %>

  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>

  <p><%= button_to "Delete", question_path(@question), method: :delete %></p>

<% end %>
