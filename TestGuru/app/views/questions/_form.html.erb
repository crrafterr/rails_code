<h1><%= question_header(test, question) %></h1>

<%= form_with model: [test, question] do |form| %>

  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>
  
<% end %>